spring:
  application:
    name: "configserver"
  profiles:
#    active: "native"
     active: git
  cloud:
    config:
      server:
#        native:
#          search-locations: "classpath:/config"
#          search-locations: "file:///D://Sai Teja//MS//microservices-4.0.0//MS//documents//config"
          git:
            uri: "https://github.com/mamidisrisaiteja/eazybytes-config.git"
            default-label: "main"
            timeout: 5
            clone-on-start: true
            force-pull: true
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    passowrd: "guest"
encrypt:
  key: "41BGTY2BFJKIO867f9I3957WDE"
server:
  port: 8071

management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    readiness-state:
      enabled: true
    livness-state:
      enabled: true
    endpoint:
      health:
        probes:
          enabled: true
